# Story 5.4: AI-Generated Motivational Messages

Status: done

## Story

As a user,
I want the motivational message shown before a quiz session to be generated by AI in my configured language and tone,
So that the encouragement feels contextual and consistent with the rest of the quiz experience.

## Acceptance Criteria

1. **Given** `screens/select-domain.ts` is updated  
   **When** the motivational message condition is met (returning user within 7 days OR score trending upward)  
   **Then** `generateMotivationalMessage(trigger, settings)` is called — a new function in `ai/client.ts` — before the quiz starts  
   **And** the message is displayed using `success()` formatting

2. **Given** `ai/prompts.ts` exports `buildMotivationalPrompt(trigger, settings)`  
   **When** called with `trigger = "returning"` and `settings = { language: "Greek", tone: "pirate" }`  
   **Then** the prompt instructs the model to generate a short (1–2 sentence) motivational message in Greek using pirate tone, acknowledging the user's return

3. **Given** `trigger = "trending"` (score trending upward)  
   **When** the prompt is built  
   **Then** it instructs the model to congratulate the user on their upward score trend in the configured language and tone

4. **Given** the Copilot API is unreachable when generating the motivational message  
   **When** `generateMotivationalMessage()` fails  
   **Then** the error is silently swallowed — no message is displayed and the quiz starts normally

5. **Given** `screens/select-domain.test.ts` is updated  
   **When** I run `npm test`  
   **Then** all tests pass, covering: AI call made when trigger met, settings passed to prompt builder, graceful degradation on API failure, no message shown on fresh domain

## Tasks / Subtasks

- [x] Add `buildMotivationalPrompt(trigger, settings)` to `src/ai/prompts.ts` (AC: 2, 3)
  - [x] Export type `MotivationalTrigger = 'returning' | 'trending'`
  - [x] Build prompt instructing model to reply ONLY with a 1–2 sentence motivational message
  - [x] Prepend voice instruction via `buildVoiceInstruction()` when settings differ from defaults
  - [x] Include trigger context: "returning" → acknowledge return; "trending" → congratulate upward trend

- [x] Add `generateMotivationalMessage(trigger, settings)` to `src/ai/client.ts` (AC: 1, 4)
  - [x] Create a session, call `sendAndWait` with the motivational prompt
  - [x] On success return `{ ok: true, data: string }` (raw message text, not JSON)
  - [x] On any error return `{ ok: false, error: string }` (caller silently ignores)
  - [x] Always disconnect session in finally block

- [x] Update `src/screens/select-domain.ts` (AC: 1, 4)
  - [x] Add `readSettings` + `defaultSettings` imports
  - [x] Add `generateMotivationalMessage` import from `ai/client.js`
  - [x] Call `readSettings()` once at top of `showSelectDomainScreen()`
  - [x] Replace static `console.log` messages with `generateMotivationalMessage` calls on both triggers
  - [x] Await and display result with `success()` only if `ok: true`; silently skip on failure

- [x] Update `src/ai/prompts.test.ts` (AC: 2, 3)
  - [x] Test `buildMotivationalPrompt('returning', defaultSettings)` contains return-acknowledgement
  - [x] Test `buildMotivationalPrompt('trending', defaultSettings)` contains trend-congratulation
  - [x] Test voice instruction injected for non-default settings
  - [x] Test English/normal settings produce no voice prefix

- [x] Update `src/ai/client.test.ts` (AC: 1, 4)
  - [x] Test `generateMotivationalMessage` returns `ok:true` with content string on success
  - [x] Test returns `ok:false` on network error (graceful)
  - [x] Test session disconnect called in all paths

- [x] Update `src/screens/select-domain.test.ts` (AC: 1, 4, 5)
  - [x] Add mocks for `generateMotivationalMessage` and `readSettings` (module-level vi.mock)
  - [x] Test AI call made on `returning` trigger, settings passed
  - [x] Test AI call made on `trending` trigger
  - [x] Test message displayed with `success()` when AI returns ok
  - [x] Test no message displayed when AI fails (graceful degradation)
  - [x] Test no AI call made on fresh domain (no trigger)
  - [x] Test existing real-file-system tests remain unaffected

## Dev Notes

### Motivational prompt design
- The model should return ONLY plain text (1–2 sentences), no JSON, no fences
- Trigger `"returning"` → instruct model to acknowledge the user's return and encourage them
- Trigger `"trending"` → instruct model to congratulate upward score trend
- Voice instruction applied as prefix (same helper as question prompts)

### Error handling
- `generateMotivationalMessage` failure → silent; quiz starts without a message
- `readSettings` failure → falls back to `defaultSettings()` (same pattern as quiz.ts)

### select-domain.test.ts approach
- Existing tests use real file system via `_setDataDir` — keep those unchanged
- New motivational-message tests switch to full module mocking (`vi.mock` for store + ai/client)
- Use a separate `describe` block with its own `beforeEach` resets for the mocked tests

### Files to modify
- `src/ai/prompts.ts` — add `buildMotivationalPrompt` + `MotivationalTrigger` export
- `src/ai/client.ts` — add `generateMotivationalMessage`
- `src/screens/select-domain.ts` — read settings, call AI, display or skip
- `src/ai/prompts.test.ts` — add motivational prompt tests
- `src/ai/client.test.ts` — add motivational message generation tests
- `src/screens/select-domain.test.ts` — add AI-path tests

## Dev Agent Record

### Implementation Notes
- `buildMotivationalPrompt` reuses `buildVoiceInstruction()` from prompts.ts for consistent language/tone injection
- `generateMotivationalMessage` returns raw plain text (not JSON) — the model is instructed to reply with only the message
- `select-domain.ts` calls `readSettings()` once per screen entry; falls back to `defaultSettings()` on read failure
- `select-domain.test.ts`: added top-level `vi.mock('../ai/client.js')` with trigger-aware mock; used `importOriginal` partial mock for `domain/store.js` to preserve real file-I/O while mocking only `readSettings`
- Used inline `{ language: 'English', tone: 'normal' }` literals in mock factory and `beforeEach` to avoid Vitest hoisting issues with imported function references

### Senior Developer Review (AI)
**Outcome:** Changes Requested | **Date:** 2026-03-15
**Action Items:** 3 fixed

- [x] [M1] Both triggers firing sequentially → doubled serial AI latency before quiz start — refactored to `Promise.all` concurrent dispatch
- [x] [L1] `trending` test assertion too loose (only `'trend'`) — added `'score'` check
- [x] [L2] `ok:true, data:''` case correct but untested — added test asserting no log on empty message; added concurrent-triggers test
- [x] [post-review] Added `ora` spinner around `Promise.all` AI call — `'Getting your message...'` displayed while awaiting results; stops before messages are printed

### Completion Notes
- All tests pass after review fixes (351/351)
- 21 tests in prompts.test.ts, 25 in client.test.ts, 22 in select-domain.test.ts

## File List
- src/ai/prompts.ts
- src/ai/client.ts
- src/screens/select-domain.ts
- src/ai/prompts.test.ts
- src/ai/client.test.ts
- src/screens/select-domain.test.ts
